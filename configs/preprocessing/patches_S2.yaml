# El programa presupone la existencia del archivo `gsa_2022_selectedtiles.gpkg
# en `in_path` y que los productos a procesar están guardados en un 
# directorio `products` dentro de `in_path`.
tile: "T31TBF"
in_path: "D:/datos/europa/raw/products" # dirección del directorio con los datos a procesar.
out_path: "D:/datos/europa/processed" # dirección del directorio donde se almacenarán los datos procesados siguiendo el formato PASTIS
patch_size: 256 
grid_padding: 0 
verbose: False
user: "f.maldonado@ug.uchile.cl"
password: ""

# 31TBF
# 29TNF
# 30UXU
# 32TPP
# 32UMC
# 29UNU
# 33TVN
# 31UFU
# 35TMH
# 32VNH
